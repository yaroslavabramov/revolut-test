import styled from 'styled-components';

const Section = styled.section`
  padding: 16px;
`;

const InputsRow = styled.div`
  display: flex;
  justify-content: space-between;
`;

export { Section, InputsRow };
